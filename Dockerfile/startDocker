
FROM python:3.12-slim


WORKDIR /app


COPY . /app


RUN pip install --no-cache-dir -r requirements.txt || echo "No requirements.txt found"


ENV TEST_VARIABLE="Default Test Value"

# Command to run the application
CMD ["python", "app.py"]